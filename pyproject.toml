[project]
name = "contravis3"
version = "0.1.0"
description = "Contra dance visualizer: LLM-parsed figures → animated bird's-eye view"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.40.0",
]

[project.scripts]
contravis = "main:main"
